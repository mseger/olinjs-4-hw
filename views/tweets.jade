extends layout

block content
	h1 Welcome to Twitter
	div
		form(name="compose_tweet", action="/tweet/new", method="post")
			| What's on your mind?
			input(type="text", name="tweet_body")
			input(type="submit", value="Tweet")
		br
		br
		each tweet, i in tweets
			div(value=tweet, id="tweet_id") 
				| #{tweet.username}
				| #{tweet.text_body}

			br 
			br
